# Add Wine Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Install `wine-staging` + `winetricks` (+ `wine-gecko`, `wine-mono`) via the existing official-packages task.

**Architecture:** Package-only group appended to `playbooks/roles/software/tasks/_pacman.yml` after `=== Archive manager ===`. No new app task, include, or pacman.conf change — Arch's new-WoW64 official wine covers 32-bit apps without multilib.

**Tech Stack:** Ansible (`community.general.pacman`), Arch extra repo.

## Global Constraints

- Packages only in `_pacman.yml` — do NOT create `tasks/wine.yml`, do NOT touch `main.yml`.
- Do NOT enable `[multilib]` (new WoW64 mode, wine ≥ 10.8).
- No new tags; installed via `--tags packages`.
- Commit message in English per repo convention.

---

### Task 1: Add Wine package group

**Files:**
- Modify: `playbooks/roles/software/tasks/_pacman.yml:172-174`

**Interfaces:**
- Consumes: nothing (existing `packages` tag).
- Produces: packages `wine-staging`, `winetricks`, `wine-gecko`, `wine-mono` installed by the `packages` tag. No later task depends on this.

- [ ] **Step 1: Append the Wine group**

In `playbooks/roles/software/tasks/_pacman.yml`, after the `=== Archive manager ===` group (the last group in the file), append:

```yaml
      # === Wine ===
      - wine-staging
      - winetricks
      - wine-gecko
      - wine-mono
```

- [ ] **Step 2: Verify static checks**

Run from repo root:

```bash
cd playbooks && ansible-playbook site.yml --syntax-check && ansible-lint
```

Expected: both pass, no new warnings.

- [ ] **Step 3: Commit**

```bash
git add playbooks/roles/software/tasks/_pacman.yml
git commit -m "packages: add wine-staging, winetricks, wine-gecko, wine-mono"
```

---

## Deployment (after merge, run by user)

```bash
cd playbooks && ansible-playbook site.yml --tags packages
```

Smoke test: `wine --version` prints `wine-11.x` (or newer); `winetricks --version` runs.

## Self-Review

- **Spec coverage:** wine-staging, winetricks, wine-gecko, wine-mono in `_pacman.yml` group — Task 1 Step 1. No multilib — nothing in plan touches pacman.conf. No new app task — nothing in plan creates it. ✓
- **Placeholder scan:** no TBD/TODO; all code shown inline. ✓
- **Type consistency:** single task; no cross-task interfaces. ✓
