---
# backup: pcmanfm saves state to this file on exit
- name: Deploy pcmanfm config
  ansible.builtin.copy:
    src: pcmanfm/pcmanfm.conf
    dest: "{{ home }}/.config/pcmanfm/default/pcmanfm.conf"
    backup: true
